<h1><%= title %></h1>

<a href="/genero/criar">Criar gênero</a>

<% if (allGenres.length > 0) { %>
  <ul>
    <% allGenres.forEach(genre => {%>
      <li>
        <a href=<%= `/genero/${genre.id}` %>>
          <%= genre.name %>
        </a>
      </li>
    <% }) %>
  </ul>
<% } else {%>
  <p>
    <i>
      Ainda não há gêneros. 
    </i>
  </p>
<% } %>

